<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.skirental">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SkiRental">
        <activity android:name=".InfoOchelariActivity"></activity>
        <activity android:name=".OchelariActivity" />
        <activity android:name=".InfoCascaActivity" />
        <activity android:name=".InfoBeteActivity" />
        <activity android:name=".CascaActivity" />
        <activity android:name=".BeteActivity" />
        <activity android:name=".InfoClapariActivity" />
        <activity android:name=".ClapariActivity" />
        <activity android:name=".InfoSkiuriActivity" />
        <activity android:name=".SkiuriActivity" />
        <activity android:name=".FirstActivity" />
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>